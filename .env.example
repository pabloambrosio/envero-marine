# Conexión a MySQL. Local: la base del Docker de `yarn db:up`.
# En producción vive solo como variable de entorno de la app Node en cPanel.
DATABASE_URL=mysql://envero:envero@127.0.0.1:3310/envero_marine

# Solo los usa scripts/seed-admin.js para crear el admin inicial.
ADMIN_EMAIL=
ADMIN_PASSWORD=
